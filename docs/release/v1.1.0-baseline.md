# Baseline pré-estabilização v1.1.0 — 2026-09-01

Medido no checkpoint `14f032b` (Node v24.19.0, Electron 40.10.6, Windows 11).

## Resultados

| Verificação | Resultado |
| --- | --- |
| `npm run check` | PASS |
| `npm test` | 23/23 PASS |
| `npm audit` | BLOQUEADO — ENOLOCK (sem package-lock.json; rodar após M3) |
| Git | 1 commit anterior, sem remote configurado |
| Segredos no repo | Nenhum (grep token/secret/password/apikey/credential) |

## Falhas preexistentes conhecidas (a corrigir nesta rodada)

Críticas: (1) `crypto`/`os` usados sem import em main.js:609-610 — polyfill de
extensões nunca injetado; (2) remover extensão apaga a pasta ORIGINAL do usuário
(main.js:824-827); (3) páginas internas abertas via omnibox/settings montam sem
preload → página vazia (main.js:1276 vs 1490-1522); (4) despacho de ações de
extensão (`__zeos_trigger_*`) nunca é enviado.

Altas: CPU/RAM sobrescritos com 0 na UI; Shift na omnibox colapsa a barra;
I/O síncrono em todo `sendState()`; favicon via google.com/s2 vaza domínios;
`Browser.downloads` = NaN; popup de extensão com `contextIsolation:false`.

Segurança (descoberto na auditoria): `setWindowOpenHandler` roteia qualquer
esquema de `window.open()` remoto para `createWebTab` (inclusive `file:`/`zeos:`);
IPC `downloads:open-file`/`show-in-folder` não valida `filePath`.

Qualidade de testes: `test/extensions.test.js` e `test/workspaces.test.js`
reimplementam a lógica inline (tautológicos — não importam o código real).

## Observações

- Os 23 testes passam ANTES das correções; qualquer quebra posterior é
  regressão introduzida, não preexistente.
- Smoke GUI de referência: página `zeos://extensions` via omnibox abre VAZIA
  (bug 3) — critério de aceitação: passar a listar extensões.
